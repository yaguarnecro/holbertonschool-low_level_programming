#include <stdio.h>

/**
 * main - puts line to stdout
 * Description: uses prinf to output fixed message
 * Return: 0
 */

int main(void)
{
        printf("ith proper grammar, but the outcome is a piece of art,\n");
        return (0);
}
